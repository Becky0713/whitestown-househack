# Generated reports

This folder starts empty. After you run the project, it will create:

- `shortlist.csv` — screened Whitestown properties ranked for the house-hack strategy
- `latest_summary.txt` — latest run summary

If you downloaded the repo before running it, not seeing `shortlist.csv` yet is normal.
